public class CanCurrency extends Currency{
        public double currencyDisplay(){
            //Figure out the currency conversion rate
            //Returning 0 to compile.
            return 0.0;
        }
    }

